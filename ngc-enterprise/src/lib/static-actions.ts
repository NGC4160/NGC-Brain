export async function createWorkOrder() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function updateWorkOrderStatus() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function updateWorkOrderStatusFromForm() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function recordInvoicePayment() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function createEstimate() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function approveEstimate() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function convertEstimateToWorkOrder() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function createCustomer() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function createPriceBookItem() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function updateInventoryReorderPoint() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function adjustInventoryStock() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function assignDispatchDriver() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function updateDispatchStatus() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function updateDispatchStatusFromForm() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}

export async function assignWorkOrderBay() {
  return { ok: false, error: "Server actions are disabled in static demo mode." }
}
