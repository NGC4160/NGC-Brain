# Omi Second Brain — Python deps (Python 3.11+)
python-dotenv>=1.0.1
requests>=2.32.0
PyYAML>=6.0.2
httpx>=0.28.0
tenacity>=9.0.0
rich>=13.9.0
python-dateutil>=2.9.0

# Local embeddings + vector search
chromadb>=0.6.0

# MCP custom server (stdio)
mcp>=1.6.0
